salario_inicial = float(input('Qual é o salário do funcionário: R$'))
salario_final = salario_inicial + (salario_inicial * 0.15)

print(f'O valor do salário após 15% de aumento fica em: R${salario_final:.2f}')
